app:
  api: openapi.json
  applicationId: ${APP_ID}
  env: ${APP_ENV:dev}
  host: ${APP_HOST:https://localhost:9000}
  excludePaths:
    - "/logout"
  antiCsrfParam: "_csrf"
hawk:
  spider:
    maxDurationMinutes: 2
